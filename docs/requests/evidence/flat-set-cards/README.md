# Flat Set cards and disk piles evidence

`tests/scenes/DuelStagingTest.tscn` windowed with `-- --capture <dir>`: the
`staging_set_*.png` review pictures, taken in the same seeded duel the first
time the opponent has a Set Spell/Trap in front of a face-up monster and a Set
monster. Before is `main` with the same test scene, after is the fix.

| File | What |
| --- | --- |
| `*_opponent_field.png` | The opponent's field from above the player's side: two face-up monsters (the right one taken with Snatch Steal), a Set monster, a Set Spell/Trap and the face-up Snatch Steal |
| `*_duel_camera.png`, `*_duel_camera_zoom.png` | The same moment from the duel camera, and the opponent cropped from it |
| `*_player_piles.png` | The player's disk from above: deck and graveyard |
