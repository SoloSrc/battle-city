# Flat Set cards evidence

Captures of `tests/scenes/DuelStagingTest.tscn` windowed with
`-- --capture <dir>`, twelve commands into the same seeded duel, on
`main` (before) and on the fix (after), downscaled to 1280 px.

| File | What |
| --- | --- |
| `before_duel_camera.png`, `after_duel_camera.png` | The duel camera |
| `before_duel_camera_zoom.png`, `after_duel_camera_zoom.png` | The field cropped from the full-size capture |
| `before_side.png`, `after_side.png` | The opponent's side from the review camera: two Set Spell/Traps and a Set monster |
