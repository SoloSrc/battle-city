# Duel effects evidence

Captures of `tests/scenes/DuelStagingTest.tscn` windowed at 1600×900 with
`-- --capture <dir>`, downscaled to 1280 px. See
[duel64-hooks.md](../../duel64-hooks.md).

| File | What |
| --- | --- |
| `staging_effects.png` | Staged SummonFlash (player centre zone), AttackTrail (to the opponent's centre zone) and HitPulse (opponent's chest), eight frames in |
| `staging_attack.png` | The seed's first `AttackDeclared`, eight frames in |
| `staging_hit.png` | The seed's first `BattleDamage`, six frames in |
| `staging.png` | The regular capture twelve commands in |
