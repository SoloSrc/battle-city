# Decision Log

One line per notable decision. Newest at the bottom. Anyone may append;
do not edit past entries, add a new one that supersedes it.

Format: `| date | decision | rationale | decided by |`

| Date | Decision | Rationale | Decided by |
| --- | --- | --- | --- |
| 2026-09-07 | Project is MIT-licensed, copyright SOLOSRC | Open proof of concept; company holds the copyright | director |
| 2026-09-07 | Roles: director/producer (human), lead programmer + game and systems designer (claude-fable), lead artist (gpt-astra) | Three-party team with clear ownership | director |
| 2026-09-07 | GitHub account SoloSrc hosts the repo; it is a user account, not an org | Existing account | director |
| 2026-09-07 | One git worktree and branch per collaborator; work reaches `main` via pull requests merged by the director | Sessions never share a working directory; director approves everything | director, claude-fable |
| 2026-09-07 | gpt-astra is also level designer and owns `levels/`; claude-fable owns tools, components and shared code | Artist composes levels with programmer's tools; recorded in AGENTS.md | director |
| 2026-09-07 | Engine: Godot 4.7 .NET with C# | Team skill and .NET tooling; installed on the host | director |
| 2026-09-07 | Fan project inspired by Pokémon BDSP exploration with Yu-Gi-Oh Goat Format duels; original IP comes after the PoC | The PoC tests the team, not the concept | director |
| 2026-09-07 | Goat Format rules, card pool (17 Aug 2005) and April 2005 banlist, per goatformat.com, including 2005-era rulings | Frozen, balanced, tractable ruleset | director |
| 2026-09-07 | One character model style everywhere: realistic-proportion anime 3D, never chibi | Less art burden; reusable asset library | director |
| 2026-09-07 | Player avatar is customisable with a chosen name | BDSP-style creator | director |
| 2026-09-07 | Duels happen in the overworld, standing, with duel disks; cards float as holograms; no arena scene | Battle City anime presentation | director |
| 2026-09-07 | PoC shows floating cards only; 3D monster holograms are post-PoC | Scope | director |
| 2026-09-07 | Card face uses the 4Kids-era anime layout with no name or text box; names and text live in the UI | Art-first look; one frame template | director |
| 2026-09-07 | Third-party reference images are linked, never committed; all committed drawings are original | Copyright | claude-fable, director |
| 2026-09-07 | Platform: desktop Windows, macOS, Linux; gamepad primary, keyboard fallback | Cheapest to ship from Godot | claude-fable (assumed), director (by merging #9) |
| 2026-09-07 | Slice: one district, three duelists, one shop, 72-card curated subset, 45–90 min | PoC scope | director (merged #11) |
| 2026-09-07 | Losing a duel costs nothing but time; boosters remain the duel reward | Friendly slice; non-deterministic progression accepted | director |
| 2026-09-07 | Squash merges to `main`; collaborator branches merge `main` back rather than rebasing | Avoids force pushes across worktrees | claude-fable |
| 2026-09-08 | Camera acceptance metric is avatar height 100 px at 1080p (90–120), tuned within the GDD's 12–14 m distance | Resolves sheet 01 inconsistency raised by gpt-astra | claude-fable, pending director |
| 2026-09-08 | Ten card anchors per side are generated from the stand points, independent of the disk prop's bay spacing | Cards stay selectable regardless of prop detail | claude-fable |
| 2026-09-08 | Card art is 512² centre-cropped to a 4:5 window with an 80 % safe column | Square art, portrait frame | claude-fable, pending director |
| 2026-09-08 | Duel engine is a Godot-free C# library driven by commands and events, tested with xUnit | Testability and determinism | claude-fable |
| 2026-09-08 | All eight documents in the "Docs ready" milestone are approved as merged; headers updated | Merge to `main` is the director's approval | director |
| 2026-09-08 | Overworld camera keeps the GDD values (55–60° pitch, 12–14 m, 35° FOV); avatar pixel height is an outcome, not a target. Supersedes the 100 px entry above | Director decision on gpt-astra's reconciliation request | director |
| 2026-09-08 | Card art crop policy decision deferred until after the Skeleton milestone; systems §6.2 remains a proposal | Not blocking the skeleton | director |
| 2026-09-08 | Next milestone is "Skeleton": the runnable project from architecture §11 plus the artist's pipeline smoke test and turnarounds | Unblocks level and asset production | director |
| 2026-09-13 | Duel engine: two consecutive priority passes advance; the engine auto-passes only windows with nothing to decide, never the turn player in a Main Phase or Battle Step; `Pass` in Main Phase 1 skips to the End Phase | Keeps the 2005 priority structure for tier 2 responses while the turn player always ends the turn explicitly | claude-fable |
| 2026-09-13 | All 72 card files are committed now; tier 2–4 cards carry stub effect ids that load and validate but cannot be duelled until the effect class exists (`DuelEngine.Start` refuses them) | Data complete for the shop, deck editor and validator before the engine tiers land | claude-fable |
| 2026-09-13 | Card art uses centered cover in the approved 520×560 window: scale 512² to 560² and clip 20 px per side, without stretching | Director approved removal of top/bottom gaps in PR #50 frame samples; supersedes the deferred crop decision, systems §6.2 | director |
| 2026-09-13 | Toon material is applied at load by `ToonMaterials.Apply` (one derived `ShaderMaterial` per `toon_*` source material, shared outline `next_pass`) instead of a glTF import script | No per-asset `.import` edits, works on already-imported files, one place to tune | claude-fable |
| 2026-09-13 | Nico duels with Rookie Beatdown: the Beatdown list with its six Level 4 attackers swapped for real vanilla monsters of 1600 ATK or less (seven new tier 1 cards, 79 total); the player keeps the full Beatdown starter | Simulation showed the tutorial AI beating random play 60/60 with the mirror list; a weaker monster line is the cheapest lever | director |
| 2026-09-13 | Directional shadow quality is set in code at level load (`SunShadows`: orthogonal 25 m, high filter, 24-bit atlas) rather than per level file | Fixed overworld camera makes one setup right for every level; the walkthrough's jagged shadow edges came from the PSSM far split and the low filter default | claude-fable |
| 2026-09-13 | Hologram cards are quads with two side materials; per-card state (hover phase, selected, reveal, dissolve) travels as instance shader parameters | Two materials for all cards; VFX hooks 6.2, 6.3 and 6.7 become parameter tweens | claude-fable |
| 2026-09-13 | Response windows are explicit engine state (`DuelState.Window`): two passes on an empty chain close the window instead of always advancing the phase; the summon window returns to the Main Phase, the attack windows drive the Damage Step | One rule for every §5.5 timing; the UI and AI read where they are from one field instead of inferring it | claude-fable |
| 2026-09-13 | Everything that asks a player something is a `PendingChoice` answered by the `AnswerChoice` command (costs, targets, optional triggers, trigger order); `LegalActions` enumerates the valid answers | The UI and the AI share one protocol; no card needs UI code; the AI's one-step lookahead works through prompts because clones copy pending state | claude-fable |
| 2026-09-13 | No attack replay: an attack whose attacker or target left the field before the Damage Step is cancelled | Replays need a second target choice mid-attack and no card in the pool depends on them | claude-fable |
| 2026-09-13 | Trigger ordering is SEGOC-lite: mandatory first (turn player's, then opponent's, each controller ordering their own), then optional ones offered one at a time in the same order; a trigger whose condition stopped holding is dropped | 2005 rules had no formal SEGOC; this is deterministic, testable and needs at most one ordering prompt per side | claude-fable |
| 2026-09-13 | Adopt the director’s Downloads/cards frame references, glossy attributes/level badges and tooltip-only subtype icons; layout revision 4 | New director art direction supersedes the previous face layout; systems §6.2 and card-reference-frames handoff | director |
| 2026-09-13 | Modifiers are one registry: timed ones from resolved effects are stored with an expiry turn, continuous ones are recomputed from face-up cards on every state change; both project onto computed ATK/DEF bonuses and restriction flags that the rules read and effects never write | One place answers "what is this monster's ATK" and "may it attack"; nothing stored can point at a card that left the field | claude-fable |
| 2026-09-13 | "Until the End Phase" effects end when the End Phase begins, "until the end of your next turn" at the End Phase of that turn number; temporary control and Spirit returns happen there too, before the End Phase triggers | One expiry rule for every timed effect; no per-card bookkeeping | claude-fable |
| 2026-09-13 | Leaving-the-field consequences (an equip's monster is gone, Snatch Steal's control returns, Premature Burial's monster is destroyed) run immediately through `IEffect.OnLeftField`, not as chain links | Rulings treat most of these as continuous effects ending; the one trigger among them (Premature Burial) gains nothing from a chain in this pool | claude-fable |
| 2026-09-13 | Tokens leave the duel the moment they leave the field; Fusion monsters return to the Graveyard like any monster (no "properly Summoned" tracking) | No card in the pool revives a Fusion monster from the Graveyard | claude-fable |
| 2026-09-13 | Card layout revision 5 supersedes revision 4: director-approved generated visual references are committed, matched 10px bevels, a shorter colored panel, aligned stars/attribute and larger upright stats; monster plates use 30% fill and orange-red borders | Preserve the approved design against visual drift; exact layout and provenance are recorded in systems §6.2 and the card-design reference archive | director |
| 2026-09-13 | Replace the swirling card back with the supplied flat tan rim, brown inset and centered outlined dark ellipse | Director requested exact attachment appearance; runtime size/path unchanged, see card-back-black-hole handoff | director |
| 2026-09-13 | Questions asked while a chain link resolves (Graceful Charity's discard, Sangan's search, Dust Tornado's Set) go through `DuelEngine.Ask`: the link parks on `DuelState.ResolvingLink`, the effect's `Resolve` re-runs from the top after each answer and reads the answers back in order, guarding earlier work with `ChainLink.Stage` | Same prompt protocol as costs and targets, clones stay cheap and replayable, no continuation objects to copy | claude-fable |
| 2026-09-13 | "Must attack if able" (Berserk Gorilla) is a restriction on `Pass`, not an automatic attack: the turn player cannot skip Main Phase 1 while the Battle Phase can be entered nor end the Battle Step while the Gorilla can still declare; flipping it face-down does not destroy it (its effect is off while face-down) | The player still chooses the target; the rule lives in one validation | claude-fable |
| 2026-09-13 | Goblin Attack Force and Giant Orc switch to Defense Position when the Battle Phase ends (not at the end of the Damage Step) and are locked until the end of their controller's next turn through the modifier registry | Matches the printed timing and reuses the position lock from #55 | claude-fable |
| 2026-09-13 | Gravekeeper's Spy summons in face-up Defense Position without asking; Fissure, Smashing Ground and Widespread Ruin let the activating player break a tie; Axe of Despair's recycle trigger fires only when it goes from the field to the Graveyard; Trap Hole answers the opponent's Normal, Tribute and Flip Summons only | Fewer prompts where the choice rarely matters, real-card rulings where they do | claude-fable |
| 2026-09-14 | Tier 3 timings are engine windows, not per-card code: `OnBattle` and `OnBattleDamage` fire after damage calculation with the battled monster in the context, `SpellActivated` fires for face-up monsters, Standby/End triggers reach the Graveyards; Chaos Sorcerer's summon is a `SummonProcedure` effect kind (costs paid, no chain link) and "cannot be Normal Summoned/Set" is a `SummonLimit` the summon rules read | The next tier (BLS) reuses the same procedure and limits; no card asks the engine for a special case | claude-fable |
| 2026-09-14 | Choices stay card ids: Tribe-Infecting Virus declares a Type by pointing at a face-up monster of that Type, Don Zaloog picks between itself (discard) and the opponent's top deck card (mill), Enemy Controller picks its mode with the optional tribute | One prompt protocol for the UI and the AI; no string or enum options | claude-fable |
| 2026-09-14 | Swords of Revealing Light stays on the field through `IEffect.RemainsOnField` and `CardInstance.LeavesAfterTurn` (destroyed at the End Phase of the opponent's third turn); Call of the Haunted attaches to its monster like an equip so either leaving takes the other; a monster flipped face-down keeps living when Premature Burial or Call of the Haunted is destroyed by the flip | Matches the printed rulings with the mechanisms of #55 instead of new bookkeeping | claude-fable |
| 2026-09-14 | Positions are not asked: Marauding Captain, the recruiters, Premature Burial, Call of the Haunted, Metamorphosis and Chaos Sorcerer summon in Attack Position; Chaos Sorcerer's attack lock is a `CannotAttack` modifier for the turn | Fewer prompts; the AI evaluates position changes next turn anyway | claude-fable |
| 2026-09-14 | The #57 acceptance duels drop the tier 4 Cyber Jar from Rookie Beatdown and Beatdown until #58 lands; `DuelEngine.Start` still refuses the full lists | Cyber Jar is tier 4 by the GDD table; the issue text overlooked it | claude-fable |
| 2026-09-14 | Thousand-Eyes Restrict's absorb moves the monster into a Spell & Trap Zone as an Equip Card (`AbsorbMonster`): absorbed monsters contribute no modifiers and fire no triggers, battle destruction of the Restrict is replaced by the absorbed monster inside `Destroy`, tokens cannot be absorbed | The only replacement effect in the pool lives in one engine check; the equip bookkeeping of #55 does the rest | claude-fable |
| 2026-09-14 | Cyber Jar summons every Level 4 or lower monster it reveals in Attack Position (Spirits and unsummonable cards go to the hand); Black Luster Soldier's two effects share one once-per-turn by checking each other's activation count; the whole pool is implemented, so the validator requires every effect id and the tier 4 Cyber Jar acceptance exception is gone | Fewer prompts; the shared limit matches the printed text without a new engine rule | claude-fable |
| 2026-09-14 | The AI rolls every candidate action out to a quiet state (chain resolved, window closed) on a clone before scoring it, answering its own prompts by the same evaluator and the opponent's by a fixed policy; the opponent's Set cards never fire in a rollout and their expected answer is the `attackRisk` term instead | One-step simulation scored activations before they resolved, so the agent never used a Spell or Trap; simulating the opponent's hidden responses would leak information | claude-fable |
| 2026-09-14 | Responses follow the systems.md §7 table (saves a monster, prevents 1000 damage, negates a Summon or activation, wins) with a non-negative profile-weighted gain, or fire on a gain of about a card; prompts wider than 48 answers use the static policy | Keeps every prompt on the one `Choice` protocol and the decision cost bounded | claude-fable |
| 2026-09-14 | Difficulty is tuned through the profiles in `data/duelists.json` with `tools/duel_sim`: Nico 1.4/0.4/1.2/0.0 jitter 1.6, Mara 1.0/1.0/0.8/0.6 jitter 1.2, Arcade Owner 1.0/1.4/0.8/1.0 jitter 0.1; the harness targets are the acceptance and CI plays a two-game smoke | The GDD §3.5 levers (jitter, playing around Set cards, bluffing) reach the targets; the old jitters were too small to tell the duelists apart | claude-fable |
| 2026-09-14 | Card views are reconciled from the engine state after every event batch (`DuelStaging.Sync`): a card's anchor and orientation come from its `CardInstance`, events only trigger the transient effects | Per-event choreography breaks on every effect that moves cards without a dedicated event (absorb, control change, jars); the state is authoritative and cheap to walk | claude-fable |
| 2026-09-14 | Defence Position turns the card 90° in its own plane (about its Z), not about Y as §6.1 first said; face-down turns it 180° about Y; the opponent's face-up cards are mirrored toward the player | A card turned about Y is seen edge-on; the shader README already said Z | claude-fable |
| 2026-09-14 | Card faces are composed once per card id in a 590 × 860 SubViewport and baked to a plain texture; the 3D hand is a row beside the body until the DuelUi hand fan (#61) takes over; duel camera values are exports on `DuelStaging` until `tuning.json` (#63) | Keeps VRAM at one texture per distinct card instead of one viewport per copy; the hand must be visible from the camera behind the duelist | claude-fable |
| 2026-09-15 | `DuelSession` drives a duel in the scene tree: the HUD submits the human's commands through it and the opponent's agent answers after a short delay; `EncounterSystem` will own one per duel (#62) | One place to pace the AI and to know the duel ended; the HUD and the staging stay observers | claude-fable |
| 2026-09-15 | The HUD derives what to ask from `LegalActions` after every command (waiting, picker, hand-limit discard, response prompt, free cursor) and builds menus with `ActionCatalog` from the command types and effect kinds; labels never come from a card id | The "no card-specific UI code" acceptance of #61; every prompt is an engine `Choice` or a list of legal commands | claude-fable |
| 2026-09-15 | Selection is a five-row cursor grid (opponent's rows mirrored) plus one list panel for menus, targets, prompts, pickers and piles; tribute sets and attack targets are second-level lists | Explicit options everywhere (GDD §3.4), one widget to make gamepad, keyboard and mouse behave alike | claude-fable |
| 2026-09-15 | The player's hand is a 2D fan in the HUD and its 3D row is hidden; the opponent's hand stays a face-down 3D row | The fan is readable and clickable at any camera distance; the 3D row was a stand-in from #60 | claude-fable |
| 2026-09-15 | The acceptance scene injects mouse events in window pixels (the viewport maps them through the stretch transform, headless windows are 64 × 64) and reads hover and picks through the same HUD code a real mouse reaches | Lets CI exercise the mouse without a window; no HUD code is test-specific | claude-fable |
| 2026-09-15 | Acceptance scenes flush the managed heap when they finish (`ManagedWrappers.Flush`): Godot .NET disposes tracked wrappers only after the engine's exit leak checks, so a resource wrapper the GC has not collected yet is reported as a leaked RID and Image; adding `DuelUi` to the assembly shifted GC pacing enough to make the district run fail every time on CI | Keeps the `^ERROR` grep of CI meaningful without ignoring leak lines; bisected on CI, no code of the HUD runs in the district scene | claude-fable |
| 2026-09-15 | A spotted player is frozen on the spot and the camera reveals the duelist with the exclamation (0.6 s blend, 1.4 s hold) before it returns and both walk to the stand points | Nico's cone reaches the room door: the player was walked away by someone off screen with no hint of what was happening (director, 2026-09-13); the player must see who spotted them first | director |
| 2026-09-15 | `DuelDirector` (`Game.Duels`) owns one `DuelStaging`, `DuelSession` and `DuelUi` for the whole run; encounters call `Begin`/`End` and wait for `Finished` | One place that stages, binds, blends the camera and tears down; card faces stay baked between duels | claude-fable |
| 2026-09-15 | The player's deck comes from `Game.Collection` (the `starter` list until the deck editor); a win adds coins and opens the reward boosters into the collection with `Game.Rng`, the same seeded RNG that hands out duel seeds; the duelist's lines and rewards come from `data/duelists.json`, the level marker keeps only the id | Runs replay from the save seed (systems.md §8, §9); no dialogue or reward text in scenes | claude-fable |
| 2026-09-15 | Tutorial hints are queued one-liners under the HUD banner, one per topic (six phases, five prompt kinds) in the first duel only; `tutorial_done` is set when that duel ends, won or lost | GDD §6 step 2 without a scripted tutorial; the HUD stays card-agnostic | claude-fable |
| 2026-09-15 | `data/tuning.json` is the single source of the systems.md §10 numbers, read through the static `Tuning.Current`; the exports that duplicated them are removed and a broken file falls back to `TuningDefinition.Default` after one error. `ai.jitter.*` and `shop.booster_price` stay in `duelists.json` and `shop.json` where they already live rather than being copied into tuning.json | One place to retune without opening scenes; two files for one number would contradict "single source" | claude-fable |
| 2026-09-15 | The autosave stores the seed plus the RNG draw count (`rng_draws`) and `Continue` skips the generator forward, instead of storing the RNG state words | Keeps the save readable and the run replayable from the seed (systems.md §8 "seed counter"); skipping a few thousand draws is instant | claude-fable |
| 2026-09-15 | Boot continues the autosave when one exists and starts a new game otherwise; New Game deletes the slot | Single slot per GDD §7 until the main menu lands (systems.md §2.2) | claude-fable |
| 2026-09-15 | The §6.4 hooks go through one adapter, `DuelEffects`, that instantiates the artist's `vfx/` scenes and plays the `assets/audio/sfx` cues; each card uniform has a single writer (the scene when it exists, the `CardView` tween otherwise), and a missing asset degrades to nothing after one warning | The #64 runtime review asked for actual anchors, side colours and no concurrent writers; presentation must never gate the rules or depend on the assets | claude-fable |
| 2026-09-15 | Acceptance scenes silence the cue players before they finish | Headless Godot never mixes audio, so an active playback is reported as a leaked AudioStream at exit and would fail the CI error grep | claude-fable |
| 2026-09-20 | Set cards lie flat (face to the ground, at the foot of the upright cards; a Set monster turned sideways) instead of standing upright with the back to the camera | Director playtest: an upright Set Spell/Trap in the front row hid the opponent's monster behind it from the duel camera (systems.md §6.1) | director |
| 2026-09-20 | On the duel disk every deck lies face to the floor, graveyards and banished piles face the sky, and piles grow upward, chosen from the marker's direction rather than from mirroring | Director: no player may see a deck's cards. The rookie disk's pile markers point down, which left the player's deck face up and their graveyard face down | director |
| 2026-09-20 | Upright cards mirror by the side they sit on, not by owner | A monster taken with Snatch Steal showed its back to the player | claude-fable |
| 2026-09-20 | Project runs prefer optional local card artwork, with generated fallback; exports remain generated-only. Duel staging/UI and district diagnostics force generated art, and other project runs can opt out with `--generated-art` | Fulfil the director’s local-download workflow while keeping committed acceptance evidence free of originals | director (runtime request), gpt-astra and claude-fable (review implementation) |
| 2026-09-20 | Director requested distinct disk/field/hand card sizes and a raised right-shoulder camera. Field 3×, disk piles 0.25×, 3D hand 1.1×; HUD hand 156 px. Duel view 40° pitch, 12° right yaw, 9 m from field midpoint. Full-field review fixture has five monsters and five Set Spell/Traps per side. | Director / gpt-astra |
